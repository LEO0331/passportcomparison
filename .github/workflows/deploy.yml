name: Deploy Flutter Web

on:
  push:
    branches:
      - main  # 確保你的主分支名稱是 main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          # 解決 SDK ^3.11.0 要求：使用最新穩定版或開發版
          channel: 'stable' 

      - name: Install dependencies
        run: flutter pub get

      - name: Build Web
        # 關鍵：將 <passportcomparison> 替換為你的 GitHub 倉庫名稱 (Repository Name)
        # 例如：https://github.com -> base-href 就要設為 "/my_app/"
        run: flutter build web --release --base-href "/passportcomparison/"

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
